# Staging Environment Values
# Overrides for the Staging environment

replicaCount: 2

image:
  pullPolicy: Always
  containerPort: 3000

ingress:
  enabled: true
  className: "modsec"
  annotations:
    external-dns.alpha.kubernetes.io/set-identifier: laa-cla-operator-app-laa-cla-operator-app-staging-green
    external-dns.alpha.kubernetes.io/aws-weight: "100"
  hosts:
    - host: laa-cla-operator-app-staging.cloud-platform.service.justice.gov.uk
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: laa-cla-operator-app-staging-tls
      hosts:
        - laa-cla-operator-app-staging.cloud-platform.service.justice.gov.uk
  whitelist:
    enabled: true

resources: {}

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 80

# Environment variables can be added here
envVars:
  BACKEND_BASE_URL:
    # Using service name instead of external domain name
    value: "http://cla-backend-app.laa-cla-backend-staging.svc.cluster.local"
  NODE_ENV:
    value: "staging"
  ENTRA_REDIRECT_URI:
    value: ""